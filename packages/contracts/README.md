# API contracts

Browser-safe request/response shapes and validation contracts only. No UI, styles, server adapters, credentials or authorization policy. Runtime exports will be added when a real API contract exists.
