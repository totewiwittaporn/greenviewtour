# Shared tooling configuration

Reserved for reusable development/build configuration. The root eslint.config.js currently owns linting. Do not store runtime secrets, business policy or UI themes here.
