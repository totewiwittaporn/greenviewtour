# Features

Group code by business capability. Each implemented feature owns its pages, API adapters and feature-only components. Shared presentation belongs in this application’s `core/ui`; application routing/providers belong in `app`. See `docs/architecture.md` for module ownership.
