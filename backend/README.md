# API

Modular backend ownership scaffold only. No HTTP server, authentication, database connection or production endpoints are implemented. Select infrastructure and implement identity-access as a separate feature.
