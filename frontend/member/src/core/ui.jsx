import {useId,useState} from 'react'
export function Button({children,busy=false,onClick,...props}){return <button type="button" onClick={onClick} aria-busy={busy} {...props}>{children}{busy&&<span aria-hidden="true"> …</span>}</button>}
export function Field({label,error,type='text',...props}){const id=useId(),[show,setShow]=useState(false);return <div className="field"><label htmlFor={id}>{label}</label><div className="field-input"><input id={id} type={type==='password'&&show?'text':type} aria-invalid={Boolean(error)} aria-describedby={id+'-error'} {...props}/>{type==='password'&&<Button className="password-toggle" aria-pressed={show} aria-label={(show?'ซ่อน':'แสดง')+label} onClick={()=>setShow(!show)}>{show?'ซ่อน':'แสดง'}</Button>}</div><span className="error" id={id+'-error'}>{error||'\u00a0'}</span></div>}
export function Select({label,children,...props}){const id=useId();return <div className="field"><label htmlFor={id}>{label}</label><select id={id} {...props}>{children}</select></div>}
export function Notice({error,children}){return children?<p className={error?'notice error':'notice'} role={error?'alert':'status'}>{children}</p>:null}
